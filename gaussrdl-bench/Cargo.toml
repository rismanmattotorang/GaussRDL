[package]
name = "gaussrdl-bench"
version = "0.3.0"
edition = "2021"
authors = ["Gaussian Technologies <research@gaussian.tech>"]
license = "MIT"
description = "Benchmark dataset registry, downloader/manager, and paper-faithful benchmark runner for GaussRDL."

[dependencies]
gaussrdl-rdl = { path = "../gaussrdl-rdl" }
serde = { workspace = true }
serde_json = { workspace = true }
reqwest = { workspace = true }
